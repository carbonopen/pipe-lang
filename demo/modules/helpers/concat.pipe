args {
    item1: String
    item2: String
}

import {
    bin "../../../target/release/libprint.so" (
        name="echo"
    )
}

pipeline {
    echo {
        text=`Result: ${ args.item1 + " " + args.item2}`
    }
}


