import {
   mod "libs/name" ()
   bin "../../target/release/liblink.so" (
        name="link"
    )

    bin "../../target/release/libprint.so" (
        name="print"
    )
    bin "../../target/release/libexit.so" (
        name="exit"
    )
}

pipeline {
    link {} :producer()

    print {
        text="Start!"
    }

    name {
        first_name="Philippe"
        last_name="Assis"
    }

    print {
        text="End!"
    }

    exit {}
}


