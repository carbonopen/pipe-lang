
vars {
    test=true
}

pipeline {
    http {
        param=true
        int=1
        attach handler {
            runtime=${ body.runtime }
            script=""
        }
    }

    mod {
        boolean=true
        int=1
        float=3.13
        string="text"
        interpolation=${ func.exec }
        attach handler
    }

}