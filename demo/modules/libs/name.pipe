import {
    bin "../../../target/release/libprint.so" (
        name="print"
    )
}

args {
    first_name: String
    last_name: String
}

pipeline {
    print {
        text="Your"
    }

    print {
        text="name"
    }

    print {
        text="is"
    }

    print {
        text=`First name: ${args.first_name}`
    }

}

