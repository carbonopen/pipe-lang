args {
    item1: String
    item2: String
    item3: String
}

import {
    bin "../../../target/release/libprint.so" (
        name="echo"
    )
}

pipeline {
    echo {
        text=`Concated`
    } :last()

    echo {
        text=`Result: ${ args.item1 + " " + args.item2 + " " + args.item3}`
    }
}


